DATABASE_URL="postgresql://postgres.SEU_REF:%24ENCODED_PASSWORD@aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require"
# Direct (may fail on IPv6-only free tier):
# DATABASE_URL="postgresql://postgres:PASSWORD@db.SEU_REF.supabase.co:5432/postgres?sslmode=require"
REDIS_URL=""
DEMO_MODE="false"
NEXT_PUBLIC_APP_URL="https://tratobarber.digitallforcelabs.cloud"
# Owner admin session (HMAC). Required in production; local fallback exists.
AUTH_SECRET="change-me-to-a-long-random-string"
# Seed password for dono@domcarlos.local (default: trato-demo)
OWNER_PASSWORD="trato-demo"
# uazapiGO — https://docs.uazapi.com/
UAZAPI_BASE_URL="https://your-subdomain.uazapi.com"
# Fallback instance token when Tenant.waInstanceId is empty
UAZAPI_TOKEN=""
# Optional — only for admin/instance management endpoints
UAZAPI_ADMIN_TOKEN=""
# Protects POST/GET /api/cron/notifications
CRON_SECRET=""
# PIX / Asaas (optional — without key, dry-run PIX + simulate button)
ASAAS_API_KEY=""
ASAAS_BASE_URL="https://sandbox.asaas.com/api/v3"
ASAAS_WEBHOOK_TOKEN=""
DEPOSIT_REQUIRED="false"
DEPOSIT_PERCENT="30"
DEPOSIT_TIMEOUT_MIN="30"
ALLOW_PIX_SIMULATE="true"
